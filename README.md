# Concurrent Ticket Booking System

This project demonstrates a ticket booking system with seat locking using Redis.

## Features
- Prevents double booking
- Uses Redis for locking
- REST API with Express

## Run
npm install
node server.js

## API
POST /api/book
GET /api/seats
